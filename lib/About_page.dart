import 'package:flutter/material.dart';

class About_page extends StatelessWidget {
  const About_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Text("hello contect"),
      );
  }
}